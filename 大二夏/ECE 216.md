# Chapter 1

### Some Basic Definitions

**Signal**：随时间或空间（或其他独立的变量）变化的物理量
**System**：对信号进行操作的物理“装置”
**Signal Processing**：接受某些输入信号，产生某些相关的输出信号

从数学角度，信号是一个单变量或多变量的函数
由函数自变量的个数，可以对信号进行分类，这一分类标准称为 **Dimensionality**，即信号的“维度”
- **One-dimensional signal**：信号函数只有一个自变量
- **M-dimensional signal**：信号函数有 M 个独立的自变量

<span class="green"> 举个例子，BW TV（黑白电视）的图像就是一个可以表示为 I(x, y, t) 的函数，x, y, t 分别代表屏幕上的横纵坐标和时间，它有3个独立的自变量，因此是一个 3D signal </span>

另一种给信号分类的方式就是函数值的维度，我们知道函数值可以是一个数值（scalar），也可以是一个向量（vector）

- **Scalar** or **Single-channel signal**：函数值是实数或复数值
- **Multichannel signal**：函数值是实数或复数向量

<span class="green"> 还是电视机的例子，对于黑白电视，其函数值是 scalar；对于彩色电视机，其函数值是三维向量，因为彩色图片是由红绿蓝三色构成 </span>

还有一种分类方式称为 **time characteristics**，根据自变量的连续与否：
- **Continuous-time signal** or **Analog signal**：A function defined for all times $t\in (-\infty, \infty)$, or at least over some continuous interval $(a, b)$.
- **Discrete-time signal**：A function defined only at certain specific values of time.

![[Pasted image 20260515192001.png]]

类似的一种分类方式叫 **value characteristics**，根据函数值的连续与否：
- **Continuous-valued signal** or **Continuous-amplitude signal**：能在一个连续区间上取任何值
- **Discrete-time signal** or **Discrete-amplitude signal**：只能取一系列离散的值

还有一种分类方式是根据信号是否可预测，可以用一个数学表达式进行表示：
- **Deterministic signals**：Can be described by an explicit mathematical representation.
- **Random signals**：Evolve over time in an unpredictable manner.

和函数一样，信号有多种表示方法：

<p align="center">
  <img src="Pasted image 20260515192104.png" width="400"/>
  <img src="Pasted image 20260515192138.png" width="200"/>
</p>

### Transformations of CT signals

这又可以分为：
- Time transformations：就是对自变量的各种变换，包括平移，数乘（伸缩），等等
- Amplitude transformations：就是对因变量的变换，对波长进行反转，缩放等操作
- More signal operations：包括微分积分等等
- 两个信号参与运算

首先第一个，Time transformations，其实就是初中数学函数题，要注意的就是当伸缩和平移混合在一起的时候，先做哪个是有一点讲究的
比如对于信号：$y(t)=x(t)$, 给他变换为以下形式：
1. $y(t)=x(at-b)$ ，这时先平移 $b$，然后伸缩 $a$
2. $y(t)=x\left( \frac{t-t_{0}}{w} \right)$ , 这时先伸缩 $\frac{1}{w}$, 然后平移
（记忆的小技巧，按照把这坨式子拆回 $t$ 的顺序，去变换）

信号系统的微分积分（复习215）：
<p align="center">
  <img src="Pasted image 20260519212256.png" width="320"/>
  <img src="Pasted image 20260519212306.png" width="320"/>
</p>

两信号的运算，以及周期这边没什么有意思的，有个辨析：
![[Pasted image 20260519212944.png]]
对于两个周期性信号，周期各自是 T1, T2, 他们的最小公倍数是两个信号和的基波周期（fundamental period)吗？
不一定，最简单的反例：两个相差半个波长的正弦波，相加直接为零，基波周期也是零

Even and old 这边也没什么要说的，就是初等函数知识

### Signal Characteristics

主要是辨析三个概念：信号的 average value, energy, average power.
简单来说：
- average value：直接对信号函数积分
- energy：信号的模平方的积分
- average power：对 energy 关于时间 average 一下

![[Pasted image 20260519213742.png]]
![[Pasted image 20260519213755.png]]
![[Pasted image 20260519213807.png]]

注意，average value = 0 不一定说明 $\int_{-\infty}^{\infty}x(t) dt$ 这个积分是有限的，只能说明它的增长速度慢于 T，并且奇函数的 average value 一定是0

更进一步的，我们定义：
- 如果 $0<P<\infty$, 那么 $E$ 一定是 infinite 的，我们称该信号是 ==power signal ==
- 如果 $E<\infty$, 那么 $P$ 一定是零，我们称该信号是 ==energy signal==
- 有些信号二者皆不是，比如 $x(t) =t^2$ , $E=\infty, P=\infty$，这种信号在工程中应用很少

Exponential Signal 和 Singularity functions，也没什么新的东西，请复习你的 215 和 286

Unit Function $U(t)$ 很好理解，就是在0处突变成1
Rect Function $rect(t)$ 就是围绕0处，总宽为1，的部分是1，其余是0

有意思的是，根据 unit function 的性质，可以用它来表示 rect function：
$$
rect(t)=u(t+1/2)−u(t−1/2)
$$
$$
rect(t)=u(t+1/2)u(1/2−t)
$$
有意思的是，如果对 $U(t)$ 做拉伸变换，比如 $U\left( \frac{t}{w} \right)$ 不改变任何，但是会拉伸 $rect(t)$ 的宽度，平移变换，减去多少，就是把中心摆到哪去

<p align="center">
  <img src="Pasted image 20260527225343.png" width="240"/>
  <img src="Pasted image 20260527225405.png" width="240"/>
</p>


**Unit Impulse Function**

这个函数就很重要了，我们在 286 就见过他
![[Pasted image 20260527225544.png]]

简单来说，$\delta(t)$ 在 0 处有一个无穷大的突出，其他部分都是0，但是定义这个突出的面积是1，因此你**在整个 x 轴上对它积分，结果一定是1** ，这也是第一条和第四条性质

关于它的变换，$\delta(t-t_{0})$ 就是把突出位置变成 $t_{0}$，然后稍微难理解的是乘以系数：
$$
\delta(at+b)=\frac{1}{|a|}\delta\left( t+\frac{b}{a} \right)
$$
这么想：你在 $\delta$ 函数里面变换，从外面看（把里面看成一个变量）它一定是要维持面积是1的，但是里面又相当于让 x 轴“覆盖”的长度放大了 $a$ 倍（啊啊我知道它理论上是0但是可以这么理解），因此相应的函数高度就要收缩 $a$ 倍

对称性，不难理解但是你经常会想不起来：
$$
\delta(t)=\delta(-t)
$$
和阶梯函数 $U(t)$ 的联系：阶梯函数求导之后，就是 $\delta(t)$
因为阶梯函数在0处一个突变到1，正好对应 $\delta$ 在0处突然面积增益1（微积分基本定理） 
反过来，对 $\delta(t)$ 积分也能得到阶梯函数，因为只有在0处有增益，其他地方无累积，所以对应了阶梯函数在其他地方都是定值

很好玩的等式：$t\delta(t)=0$ 恒成立，也许在后面的函数化简中有用呢？

非常重要的结论：

![[Pasted image 20260527225613.png]]
![[Pasted image 20260527225622.png]]

前一条不难理解，因为 $\delta(t-t_{0})$ 表示他根本不在意你 $x(t)$ 在其他地方是多少，只有 $t_{0}$ 处的值可以被反应出来，于是得到的结果就是一个新的 Impulse，还是在 $t_{0}$ 处，只是高度（增益）变成了 $x(t)$ 在此处的函数值 $x(t_{0})$
那么第二条就是对第一条的两侧积分，**这条性质是下一个chapter的核心**

直观理解第二条性质，就是以一种更骚的操作获取一个函数在某个位置的函数值，你问我函数 $x(t)$ 在 $t_{0}$ 是多少？先对整个函数施加一个 $t_{0}$ 处的 impulse $\delta(t_{0})$, 然后整个积分一遍，结果就是 $t_{0}$ 处的函数值


#### Continuous-time systems

![[Pasted image 20260528143230.png]]

记住了：一个信号是一个函数，而一个系统是对信号函数的一种变换，换句话说，是一个函数到另一个函数的映射关系

我们又要来分类了：

- **Memoryless/Static System**: Output depends only on the **present** input
- **Dynamic system**: If not static system
- **Causal System**:  Output depends only on the **present and past** times input
(Static system must be causal)

Linearity 和 Invertibility 都顾名思义，不再解释

**Stability** 指的是：对于一个有界的输出，输出也能保持有界：

![[Pasted image 20260528150353.png]]

**Time-invariance**

![[Pasted image 20260528150801.png]]

