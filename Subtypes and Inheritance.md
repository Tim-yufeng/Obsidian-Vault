
## 子类型、继承与虚函数 (Subtypes, Inheritance & Virtual Functions)

### 1. 子类型与替换原则 (Subtypes & Substitution Principle)
* **定义**：若 $S$ 是 $T$ 的子类型（记作 $S <: T$），则在任何需要 $T$ 的地方都可以用 $S$ 替代，且不影响计算的正确性。
* **替换原则 (Substitution Principle)**：为基类 $T$ 编写的代码，在使用派生类 $S$ 时依然必须是正确的。
* **对比“类型转换”**：
    * `int` 转 `double` 会改变物理表示（内存布局变化）。
    * 子类型使用 supertype 接口时，**不发生转换**，对象保持原样使用。
* **实例**：`ifstream` 是 `istream` 的子类型，因此 `add(istream &source)` 函数可以接受 `ifstream` 对象。

---

### 2. 创建子类型的三种方式
1.  **增加新操作**：子类型拥有超类型的所有方法，并额外提供新功能。
2.  **强化后置条件 (Strengthen Postcondition)**：
    * 在完成原方法承诺的基础上，提供额外的保证（如：除了计算结果，还负责打印日志）。
3.  **削弱前置条件 (Weaken Precondition)**：
    * 对输入的要求更宽松。例如原方法要求输入“正偶数”，子类型允许输入“所有整数”。

---

### 3. C++ 继承机制 (Inheritance)
* **语法**：`class Derived : public Base { ... };`
* **派生类 (Derived Class)**：继承基类的所有成员（数据和函数），并可添加新成员或重定义旧成员。
* **访问权限控制**：
    * **public 继承**：基类的 public 成员在子类仍为 public。
    * **private 成员**：基类的私有成员在子类中**不可见**。
    * **protected (受保护)**：**专门为继承设计**。成员对本类及所有派生类可见，但对外部用户隐藏。

---

### 4. 继承中的易错点：脆弱的派生类
* **风险**：过度依赖 `protected` 成员会破坏封装。
* **代价**：如果基类的底层实现改变（如 `IntSet` 从有序数组改为无序），直接访问基类数组的派生类方法（如 `MaxIntSet::max()`）可能会悄悄失效且编译不报错。

---

|**基类成员访问级别**|**公有继承 (public)**|**私有继承 (private)**|**保护继承 (protected)**|
|---|---|---|---|
|**public**|在派生类中保持 **public**|在派生类中变为 **private**|在派生类中变为 **protected**|
|**protected**|在派生类中保持 **protected**|在派生类中变为 **private**|在派生类中保持 **protected**|
|**private**|**不可见** (无法直接访问)|**不可见** (无法直接访问)|**不可见** (无法直接访问)|

### 5. 虚函数与多态 (Virtual Functions)
* **静态类型 (Apparent Type)**：变量声明时的类型（编译器看到的）。
* **动态类型 (Actual Type)**：对象在内存中真实的类型（运行时存在的）。
* **非虚函数的陷阱**：
    * 默认情况下，C++ 根据**静态类型**调用函数。
    * 如果用 `IntSet &r` 引用一个 `PosIntSet` 对象并调用非虚函数 `insert`，执行的是基类的逻辑，这可能破坏子类的约束（如插入了负数）。
* **virtual 关键字**：
    * 告诉编译器：该函数可能被重写，请在**运行时**根据对象的真实类型决定调用哪个版本。
    * **遗传性**：一旦基类声明为 `virtual`，所有派生类的同名函数自动成为虚函数。

---

### 6. 虚函数原理：vtable 与指针
* **vtable (虚函数表)**：编译器为每个拥有虚函数的类创建一个表，存放指向对应函数实现的指针。
* **vptr (虚表指针)**：每个类实例都会额外存储一个指针，指向该类对应的 vtable。
* **调用逻辑**：通过引用或指针调用虚函数时，程序先找对象的 vptr，再查 vtable，最后跳转到正确的代码地址。

> 课件第49-52页有vtable 结构图与 foo/bar 对象指向不同虚表的连线图

---

### 重点总结 
* **子类 != 子类型**：在 C++ 中，你可以通过继承创建一个子类（Subclass），但如果它违反了替换原则（如 `PosIntSet` 拒绝基类允许的负数输入），它就**不是**一个合格的子类型（Subtype）。
* **实现建议**：
    * 倾向于使用 `public` 接口访问基类数据，而非 `protected`，以降低耦合。
    * 当需要通过基类指针或引用操作子类对象时，**务必使用虚函数**。